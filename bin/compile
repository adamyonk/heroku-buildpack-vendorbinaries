#!/usr/bin/env bash

indent() {
  sed -u 's/^/       /'
}

echo "-----> Installing dependencies"

cd $1/vendor

echo Downloading taglib | indent
curl -L https://s3.amazonaws.com/adamyonk-heroku-binaries/taglib-1.8.tar.gz -s -o - | tar xzf -
